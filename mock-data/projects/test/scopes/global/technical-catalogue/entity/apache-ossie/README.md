# Apache Ossie entity package

JSON semantic_model for scope `global` (`0.2.0.dev0`). Shown in Contracts as the Ossie Semantic Model; datasets feed the Semantics KG and Data Products (SDP / ADP / CDP).

Spec: https://github.com/apache/ossie/blob/main/core-spec/spec.md
