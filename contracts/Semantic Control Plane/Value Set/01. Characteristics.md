# Value Set — Characteristics

Contract: `ctr-scp-type-value-set` · Layer: **registry**

Controlled set of allowed values for a property or metric dimension.

## Core

See [`contract.json`](contract.json) `characteristics` array.

## Hierarchy

Contains value Concepts or literal members; bound to property Concepts

## Cross-tool notes

- **semantic_control_plane:** Value Set (SoR)
- **dbt_semantic_layer:** enum / accepted values
- **collibra:** Code Value / list (source)
- **openmetadata:** Enum / tag options (source)
- **dataplex:** —
- **microsoft_purview:** Classification / enum patterns
