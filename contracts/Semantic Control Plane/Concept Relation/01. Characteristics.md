# Concept Relation — Characteristics

Contract: `ctr-scp-type-concept-relation` · Layer: **registry**

First-class typed relationship between two Concepts inside the registry.

## Core

See [`contract.json`](contract.json) `characteristics` array.

## Hierarchy

Typed edge between Concepts (specializes, partOf, measures, …); distinct from Federation Edge

## Cross-tool notes

- **semantic_control_plane:** Concept Relation (SoR)
- **dbt_semantic_layer:** entity relationships / foreign keys in semantic YAML
- **collibra:** — (asset relations are catalog-side)
- **openmetadata:** —
- **dataplex:** —
- **databricks_uc:** —
