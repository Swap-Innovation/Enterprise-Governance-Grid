# Ontology Package — Characteristics

Contract: `ctr-scp-type-ontology-package` · Layer: **registry**

Versioned, publishable set of Concepts (ontology / semantic model package).

## Core

See [`contract.json`](contract.json) `characteristics` array.

## Hierarchy

Versioned bundle of Concepts within one or more Namespaces

## Cross-tool notes

- **semantic_control_plane:** Ontology Package (SoR)
- **dbt_semantic_layer:** Semantic model / YAML package
- **databricks_uc:** Metric views package patterns
- **collibra:** —
- **openmetadata:** —
- **dataplex:** —
