# Data Model — Characteristics

Contract: `ctr-biz-type-data-model` · Layer: **semantic**

Logical data model containing entities and attributes.

## Core

See [`contract.json`](contract.json) `characteristics` array (generated; keep in sync).

## Hierarchy

Contains Data Entity; model itself rarely mapsTo

## Cross-tool notes

- **collibra:** Data Model (`Data Structure > Data Model`)
- **dataplex:** Entry (logical system/model)
- **openmetadata:** DatabaseSchema / custom model
- **microsoft_purview:** —
- **alation:** —
- **informatica_cdgc:** Model
